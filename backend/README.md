# Smart Todo Backend
Generated backend scaffold matched to your frontend.
## How to use
1. Copy `.env.example` to `.env` and fill DB credentials and JWT_SECRET.
2. Run `npm install`
3. Run `node src/server.js` (or `npm run dev` if you have nodemon)
